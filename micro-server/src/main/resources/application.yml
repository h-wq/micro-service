spring:
  application:
    name: micro-service
  profiles:
    active: online

server:
  port: 8088

mybatis:
  mapper-locations: classpath:mapping/**/*.xml
  type-aliases-package: com.hello.micro.service.model
